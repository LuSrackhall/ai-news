# v4.4 — Event 聚类 + 周报 + 反馈收集 + RSSHub 连接池

## 概述

v4.4 在 v4.2 双 Runtime 架构基础上，新增四大能力：

1. **Event 聚类** — Ingestion 阶段自动将相似事件归类
2. **Weekly Pipeline** — 按周聚合事件，生成周报
3. **反馈收集** — 记录用户交互数据（只存不学）
4. **RSSHub 连接池** — 池化 URL + 熔断器，接入 8 个无原生 RSS 的源

## 新增文件

```
scripts/
  domain/
    cluster.mjs                         # ClusterPolicy（纯计算）
  tasks-ingestion/
    cluster-events.mjs                  # ClusterEvents Task
  tasks-weekly/
    load-week-events.mjs                # 加载 7 天事件
    aggregate-by-cluster.mjs            # 按 Cluster 聚合
    generate-weekly-article.mjs         # LLM 生成周报
    render-weekly.mjs                   # 渲染 Markdown
    archive-weekly.mjs                  # 写入文件系统
  pipelines/
    weekly.mjs                          # Weekly Pipeline 声明
  repositories/sqlite/
    cluster-repository.mjs              # Cluster 写模型
    feedback-repository.mjs             # Feedback 写模型
  read-models/sqlite/
    cluster-read-model.mjs              # Cluster 读模型
  run-weekly.mjs                        # Weekly 入口脚本
  infrastructure/
    rsshub-pool.mjs                     # RSSHub 连接池 + 熔断器
  repositories/sqlite/
    weekly-report-repository.mjs        # WeeklyReport 写模型
  test-rsshub-pool.mjs                  # RsshubPool 测试（10 项）
```

## 修改文件

| 文件 | 变更 |
|------|------|
| `scripts/infrastructure/database.mjs` | 新增 event_clusters、weekly_reports、feedback 三张表 |
| `scripts/config.mjs` | 新增 chinese_tech 中文实体词典 |
| `scripts/pipelines/ingestion.mjs` | ExtractEntities 后插入 ClusterEvents |
| `scripts/run-ingestion.mjs` | 注册 ClusterEvents + 注入 ClusterRepository/ReadModel |
| `scripts/tasks-ingestion/store-events.mjs` | 写入 cluster_id + event_clusters 表 |
| `scripts/read-models/sqlite/event-read-model.mjs` | 新增 findByCluster |
| `scripts/infrastructure/scope.mjs` | buildScope 注入 cluster/feedback 依赖 |
| `scripts/test-sqlite.mjs` | 新增 9 个 v4.4 测试（共 21 个） |
| `scripts/collect-rss.mjs` | 集成 RSSHub 连接池 + RSSHub 源间隔调整 |
| `.claude/skills/ai-daily/SKILL.md` | 新增 Weekly Pipeline 触发条件和流程说明 |

## 聚类算法

ClusterPolicy 使用三重匹配（满足任一即归类）：

1. **实体重叠度 ≥ 0.5** — 交集 / 并集
2. **事件指纹相同** — Entity|EventType|Keywords|Week 哈希
3. **标题 bigram 相似度 ≥ 0.7** — 字符级 bigram Jaccard

Cluster 标题 = 该 Cluster 中 rank_total 最高的 Event 标题。

## Ingestion 流程（更新后）

```
Collect → Normalize → Verify → ExtractEntities → ClusterEvents → Score → Dedup → Store
```

## Weekly Pipeline 流程

```
LoadWeekEvents → AggregateByCluster → GenerateWeeklyArticle → RenderWeekly → ArchiveWeekly
```

输出：`output/weekly/<start>_<end>/` (article.md + script.md + manifest.json)

## RSSHub 连接池

```
RSSHUB_INSTANCES = [5 个公共实例 URL]
```

熔断器三态：CLOSED → 连续失败 3 次 → OPEN（冷却 10min × 2^n，上限 2h）→ HALF-OPEN（试探）→ 成功 → CLOSED

健康状态持久化到 `data/rsshub-health.json`。

RSSHub 源配置：`rsshub: '/anthropic/news'`（路由路径，不含实例域名）

## RSS 源扩展（25 → 33）

新增 8 个 RSSHub 中转源：Anthropic（news + research）、DeepSeek、机器之心、虎嗅、晚点LatePost、Cursor、Ollama

新增 4 个直接 RSS 源：Google AI Blog、Google Research、Meta Engineering、HuggingFace Blog、TLDR AI、LangChain Blog、Simon Willison、量子位、LessWrong、Alignment Forum

## 3-agent 评审修复

| 类别 | 问题 | 修复 |
|------|------|------|
| A1 | clusterRepository.findAll() 阻断性崩溃 | → 改用 clusterReadModel |
| A2 | weekKey() 跨年周计算错误 | → ISO 8601 标准算法 |
| A3 | fingerprint 分隔符碰撞风险 | → `\x00` 分隔符 |
| A4 | ArchiveWeekly 不写 weekly_reports 表 | → 新增写入逻辑 |
| B1 | simpleHash 注释写 SHA-256 误导 | → 修正注释 |
| B2 | SKILL.md 流程缺 cluster 步骤 | → 补充 |
| B3 | 中文实体词典遗漏 17 个 | → 阶跃星辰/可灵/面壁等 |
| B4 | feedback 表缺外键 | → REFERENCES 约束 |
| B5 | ClusterPolicy 硬编码阻碍替换 | → scope DI 注入 |

## SQLite Schema 新增

```sql
event_clusters (id, title, type, importance, event_count, entity_keys, topic_keys, first_seen, last_updated, metadata)
weekly_reports (id, week_start, week_end, cluster_count, event_count, article_chars, script_chars, created_at, metadata)
feedback (id, event_id, cluster_id, type, value, source, created_at)
```

## 使用

```bash
# Ingestion（新增 ClusterEvents 步骤）
node scripts/run-ingestion.mjs

# Weekly Pipeline
node scripts/run-weekly.mjs
node scripts/run-weekly.mjs --week 2026-06-26

# 测试
node scripts/test-sqlite.mjs  # 21 tests
node scripts/test-rsshub-pool.mjs  # 10 tests
```

## 设计原则

- **Runtime 边界不变** — v4.4 不修改 Runtime 核心，只新增 Task + Pipeline
- **聚类算法可替换** — ClusterPolicy 是纯函数，v4.5 可用 LLM 替换
- **反馈只存不学** — feedback 表存原始数据，v4.5 Learning Engine 消费
- **中文实体增强** — 新增 36氪、智谱、百川等中文 AI 生态实体

## 版本链

v4.0 → v4.1 → v4.2 → **v4.4**

v4.3 设计目标已合并入 v4.4。
