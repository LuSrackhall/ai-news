# ─────────────────────────────────────────────
# 环境变量配置模板
# 复制为 .env 后填入实际值：
#   cp .env.example .env
# ─────────────────────────────────────────────

# ── MiMo TTS（语音合成）──
#
# 两种计费方式，选一种：
#
#   Token Plan（推荐，tp- 前缀）：
#     在 https://mimo.mi.com → Token Plan → Console 获取
#     Base URL 自动识别（中国区默认）
#
#   Pay-as-you-go（sk- 前缀）：
#     在 https://mimo.mi.com → API Keys 获取
#
MIMO_API_KEY=

# 音色：冰糖（女·默认）/ 茉莉（女）/ 苏打（男）/ 白桦（男）
MIMO_TTS_VOICE=苏打

# 风格指令（自然语言，MiMo TTS 独有）
MIMO_TTS_STYLE=新闻播报风格，语速适中，沉稳大气

# ── 可选：覆盖 Base URL（一般不需要，脚本会根据 key 前缀自动识别）──
# MIMO_BASE_URL=https://token-plan-cn.xiaomimimo.com/v1
